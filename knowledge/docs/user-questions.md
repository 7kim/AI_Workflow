# User Questions

Questions about this project — add them here and the agent will answer them at session start.
Answered questions are removed from this file and archived with answers in `~/AI_Workflow/knowledge/questions/<project-name>.md`.

**Format**: one question per line or bullet.

---

- Example: Why is the WebSocket wrapped in a useRef instead of useState?
- Example: What does the `backoff` parameter do in bot_config.json?
- Example: Is the database connection pooled or created per request?

---

*Processed by agent at session start. Answered questions move to `knowledge/questions/<project-name>.md`.*
*Unanswered questions (need more context) stay here with a `<!-- TODO: needs investigation -->` marker.*
