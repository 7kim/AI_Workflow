---
description: Pipeline lifecycle — submit, list, or execute pipelines
---

Run bin/h-pipeline $ARGUMENTS. Subcommands: submit (with --planner --prompt --plan --tasks), list, execute <pipeline-id>. Calls dashboard API if running, falls back to direct file update.
