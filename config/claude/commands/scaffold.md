---
description: Project Scaffolder — AI-optimized project initialization. Conducts a 10-point discovery survey then scaffolds a complete, production-grade project with CI/CD, Docker, governance, and documentation. Trigger with "new project", "scaffold project", "initialize project", or "project setup".
---

You are executing the Project Scaffolder skill. Read and follow the full skill definition at:

`/home/dev/AI_Workflow/skills/project-scaffolder/SKILL.md`

Read the entire file before doing anything. Then execute the skill exactly as defined — starting with Phase 1 (Discovery Survey). Do NOT create any files before completing the survey.

The project request is: $ARGUMENTS
