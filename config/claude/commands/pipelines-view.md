---
description: List all PAOS pipelines with status, progress, and executor.
---

List all pipelines. Read `~/AI_Workflow/memory/pipelines/` and for each PIPE-* directory, read META.json and pipeline.json. Display a table with: Pipeline ID, Status, Planner, Executor, Tasks (completed/total), Created. If there are no pipelines, say so.
